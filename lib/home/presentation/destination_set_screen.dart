import 'package:flutter/material.dart';

class DestinationSetScreen extends StatelessWidget {
  const DestinationSetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
